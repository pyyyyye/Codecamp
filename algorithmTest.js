// Nums 라는 Integer로 되어 있는 정렬된 Array가 있습니다.
// 중복된 값을 없앴을 때 Array 길이를 반환하는 함수를 작성하세요

// Example)
// Input : [1,2,2,3,4,4,5,6]
// Output: 6

// Input: [0,0,0,4]
// Output: 2

let arr = [1, 2, 2, 3, 4, 4, 5, 6];

for (let i = 0; i < arr.length; i++) {
  const AAA = arr[i];

  for (let x = 0; x < arr.length; x++) {
    if (AAA == arr) {
    }
  }
}
